version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gogo-api-server
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
